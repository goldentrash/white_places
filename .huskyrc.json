{
  "hooks": {
    "pre-commit": "yarn run lint-staged --verbose && echo \"\" && yarn --cwd client run lint-staged --verbose && echo \"\" && yarn --cwd api run lint-staged --verbose && echo \"\"",
    "pre-rebase": "exit 1"
  }
}
