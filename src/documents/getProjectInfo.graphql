query GetProjectInfo($id: ID!) {
  project(id: $id) {
    title
    isActive
    repositoryUrl
    homepageUrl
    isFollowed
    numOfFollowers
    numOfGeneratedPoint
    numOfInformations
    numOfOpinions
    numOfTasks
  }
}
