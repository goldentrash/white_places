#import "../fragment/titleAndSummary.graphql"

mutation changeProjectTitle($input: ChangeProjectTitleInput!) {
  chagneProjectTitle(changeProjectTitleInput: $input) {
    code
    message
    project {
      ...TitleAndSummary
    }
  }
}
