{
  "name": "white_places",
  "version": "1.0.0",
  "private": true,
  "repository": "https://github.com/goldentrash/netlify_test.git",
  "author": "goldentrash <startoflateness@gmail.com>",
  "lint-staged": {
    "./*.{json, toml}": "yarn prettier --write"
  },
  "scripts": {
    "install:all": "yarn install && yarn --cwd client install && yarn --cwd api install",
    "start:dev": "yarn run build && yarn netlify dev",
    "build": "yarn --cwd client run build && yarn --cwd api run build"
  },
  "dependencies": {},
  "devDependencies": {
    "husky": "^4.3.8",
    "lint-staged": "^10.5.4",
    "netlify-cli": "2.54.0",
    "prettier": "^2.2.1",
    "prettier-plugin-toml": "^0.3.1"
  }
}
