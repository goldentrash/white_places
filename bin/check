#!/bin/sh

# Lint
yarn run lint

if [ $? -ne 0 ]
then
  echo "lint error"
  exit 1
fi

# Unit Test
yarn run test:unit

if [ $? -ne 0 ]
then
  echo "Unit Test Failed"
  exit 1
fi

# Format
yarn run format

if [ $? -ne 0 ]
then
  echo "Format Failed"
  exit 1
fi